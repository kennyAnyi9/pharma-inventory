# Database (copy from apps/web/.env.local)
DATABASE_URL=postgresql://[user]:[password]@[host]/[database]?sslmode=require

# ML Service Config
ENVIRONMENT=development
ML_API_KEY=your-secret-api-key-change-this